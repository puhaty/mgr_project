# Custom partition table (16MB flash, ESP32-S3).
# Enlarged 'factory' app partition to fit the eco-tree image assets (~2.4MB)
# with headroom for further gamification assets. NVS offset/size kept identical
# to the previous table so stored Wi-Fi credentials and RTC data are preserved.
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x600000,
